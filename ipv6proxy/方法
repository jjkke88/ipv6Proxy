##
#@function 作用：使用本校的ipv6免流量资源去访问原本使用ipv4的网络资源以达到省流量的方式
#设计方案：
#将本地的http请求(网址)转发至本地的代理端，代理端将该请求以ipv6的方式发送到服务器上，同时服务器接受到了这个指令后，将开启一个ipv4socket，访问并读取到http资源后，将所得字符串返回，再由ipv6socket返回，由此达到ipv4转ipv6
#同时还能用这种方式实现翻墙

#第一步
#创建一个与服务器之间的ipv6socket
